FROM python:3.10-slim

WORKDIR /app

COPY pyproject.toml .
COPY src/ src/

RUN pip install --no-cache-dir ".[serving]"

EXPOSE 8000

CMD ["uvicorn", "g2n.serving.app:app", "--host", "0.0.0.0", "--port", "8000"]
