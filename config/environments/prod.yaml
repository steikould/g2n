# G2N Production Environment Configuration
environment: prod

s3:
  bronze_path: s3a://g2n-prod/bronze/
  silver_path: s3a://g2n-prod/silver/
  quarantine_path: s3a://g2n-prod/quarantine/
  features_path: s3a://g2n-prod/features/

snowflake:
  warehouse: G2N_PROD_WH
  database: G2N_GOLD
  schema: PUBLIC

mlflow:
  tracking_uri: http://mlflow-prod.internal:5000
  experiment_name: g2n-prod

quality_gate:
  critical_threshold: 40.0

serving:
  host: 0.0.0.0
  port: 8000
  reload: false
